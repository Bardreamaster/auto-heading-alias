# Auto heading alias

This plugin automatically adds display text when creating heading links.

Example:

When you create a heading link like `[[File#Heading]]`, the plugin will automatically add the display text "Heading" to the link, so it will be modified as `[[File#Heading|Heading]]`.
